# Shared ignores for Railway (root Dockerfile → app/) and CI
# (deployment/Dockerfile with context `.`).
#
# Do NOT ignore Cargo.toml / Cargo.lock / Rocket.toml / src / projects —
# the backend image build copies the Rust crate from the repo root.
node_modules
**/node_modules
dist
build
**/build
.git
.vercel
target
deployment
helm
app/node_modules
