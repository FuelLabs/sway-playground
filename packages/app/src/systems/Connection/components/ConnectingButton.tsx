import { Button } from "@fuel-ui/react";

export function ConnectingButton() {
    return (
        <Button
            type="button"
            color="gray"
            isDisabled
        > CONNECTING...
        </Button>
    );
}
