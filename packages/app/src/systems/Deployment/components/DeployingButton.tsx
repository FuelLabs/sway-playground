import { Button } from "@fuel-ui/react";

export function DeployingButton() {
    return (
        <Button
            type="button"
            variant="outlined"
            size="lg"
            color="gray"
            isDisabled
        > Deploying...
        </Button>
    );
}
